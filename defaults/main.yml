---
# default vars for freebsd_bash

freebsd_bash_debug: false
freebsd_bash_install: true

freebsd_bash_install_retries: 10
freebsd_bash_install_delay: 5
freebsd_bash_install_packages:
  - shells/bash

freebsd_bash_users: []

# EOF
...
